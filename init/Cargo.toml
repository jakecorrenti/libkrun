[package]
name = "init"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "init"

[dependencies]
anyhow = "1.0"
libc = "0.2"
rustix = { version = "1.1", features = ["fs", "mount", "net", "process", "stdio", "system"] }
serde_json = "1.0"
